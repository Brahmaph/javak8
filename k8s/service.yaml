apiVersion: v1
kind: Service

metadata:
  name: javak8s:v1

spec:

  type: NodePort

  selector:
    app: javak8s:v1

  ports:

    - protocol: TCP

      port: 8080

      targetPort: 8080

      nodePort: 30080