apiVersion: v1
kind: Service

metadata:
  name: javak8:v1

spec:

  type: NodePort

  selector:
    app: javak8:v1

  ports:

    - protocol: TCP

      port: 9090

      targetPort: 8080

      nodePort: 30080